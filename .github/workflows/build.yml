name: Build

on:
    push:
        branches: ["main"]
        paths-ignore:
            - "**/README.md"
            - "**/.gitignore"
            - "doc/**"
            - "bin/**"

permissions:
    contents: write

jobs:
    Build:
        runs-on: ubuntu-latest

        steps:
            - name: Set up Go
              uses: actions/setup-go@v6
              with:
                  go-version: stable

            - name: Check out code into the Go module directory
              uses: actions/checkout@v6
              with:
                  fetch-depth: 0

            - name: Get Version
              run: echo "CLIENT_VERSION=$(make client-version)" >> $GITHUB_ENV

            - name: Build
              run: GOOS=darwin GOARCH=arm64 make build-tetris

            - name: Upload Release Asset
              uses: softprops/action-gh-release@v1
              with:
                  tag_name: client/${{ env.CLIENT_VERSION }}
                  name: Terminal Tetris ${{ env.CLIENT_VERSION }}
                  generate_release_notes: true
                  files: bin/tetris
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
